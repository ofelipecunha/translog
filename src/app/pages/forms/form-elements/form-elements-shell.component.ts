import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-form-elements-shell',
  imports: [RouterOutlet],
  template: '<router-outlet />',
})
export class FormElementsShellComponent {}
